@model Library.Web.ViewModels.EditLibraryItemViewModel

@if (Model.DigitalMediaItem != null)
{
    await Html.RenderPartialAsync("_DigitalMediaEdit", Model);
}

else
{
    await Html.RenderPartialAsync("_NonDigitalMediaEdit", Model);
}