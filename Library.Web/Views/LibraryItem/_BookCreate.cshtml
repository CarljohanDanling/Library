@model Library.Web.ViewModels.CreateLibraryItemViewModel

<div class="book box">
    @using (Html.BeginForm("Create", "LibraryItem"))
    {
    <div>
        @Html.ValidationSummary(true, "ValidationSummary")
        <ol>
            <li>
                @Html.LabelFor(m => m.Book.Title)
                @Html.TextBoxFor(m => m.Book.Title)
                @Html.ValidationMessageFor(m => m.Book.Title)
            </li>
            <li>
                @Html.LabelFor(m => m.Book.Author)
                @Html.TextBoxFor(m => m.Book.Author)
                @Html.ValidationMessageFor(m => m.Book.Author)
            </li>
            <li>
                @Html.LabelFor(m => m.Book.Pages)
                @Html.TextBoxFor(m => m.Book.Pages, new { @type = "number" })
                @Html.ValidationMessageFor(m => m.Book.Pages)
            </li>
        </ol>
        @Html.DropDownListFor(m => m.Book.CategoryId, new SelectList(Model.Categories, "Id", "CategoryName"))
        <input type="hidden" name="option" value="book" />
        <input type="submit" value="Create" />
    </div>
    }
</div>